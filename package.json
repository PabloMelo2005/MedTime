{
  "name": "medtime",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "scripts": {
    "vercel-build": "echo Build concluído",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "22.x"
  }
}
